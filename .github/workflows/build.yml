name: EAS Build on PR Opened

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run EAS workflow
        run: eas workflow:run create-production-builds.yml --non-interactive
